<%# Improves SEO by reinforcing meaningful page titles. Ensures consistency between the <title> and <h1>. Prevents empty headings %>
<% if (typeof title !== 'undefined' && title) { %>
  <h1><%= title %></h1>
<% } else { %>
  <h1>Untitled Page</h1>
<% } %>

<main>
  <h1>500 - Server Error</h1>
  <% if (typeof errorDetails !== 'undefined' && errorDetails) { %>
    <p><strong>Dev Info:</strong> <%= errorDetails %></p>
  <% } %>
</main>
